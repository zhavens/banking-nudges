export const environment = {
  production: false,
  static: false,
  secure: false,
};

/*
 * For easier debugging in development mode, ignore zone related error stack
 * frames such as `zone.run`, `zoneDelegate.invokeTask`.
 */
import 'zone.js/plugins/zone-error'; // Included with Angular CLI.

